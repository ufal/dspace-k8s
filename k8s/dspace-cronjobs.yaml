# DSpace CronJobs for Kubernetes

---
# Index Discovery - Daily at midnight
apiVersion: batch/v1
kind: CronJob
metadata:
  name: dspace-index-discovery
spec:
  schedule: "0 0 * * *"
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          securityContext:
            runAsNonRoot: true
            runAsUser: 1100
            fsGroup: 1100
            seccompProfile:
              type: RuntimeDefault
          volumes:
          - name: local-config
            configMap:
              name: local-config-map
          containers:
          - name: dspace-cli
            image: dataquest/dspace:dspace-7_x
            command: ["/bin/sh", "-c"]
            args:
              - /dspace/bin/dspace index-discovery -b
            resources:
              requests:
                cpu: 1000m
                memory: 1Gi
              limits:
                cpu: 2
                memory: 2Gi
            securityContext:
              allowPrivilegeEscalation: false
              capabilities:
                drop: ["ALL"]
              runAsNonRoot: true
              runAsUser: 1100
            volumeMounts:
            - name: local-config
              mountPath: /dspace/config/local.cfg
              subPath: local.cfg
            env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: dspace-postgres-superuser
                  key: password

---
# Health Report - Daily at midnight (ENABLED)
apiVersion: batch/v1
kind: CronJob
metadata:
  name: dspace-health-report
spec:
  schedule: "0 0 * * *"
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          securityContext:
            runAsNonRoot: true
            runAsUser: 1100
            fsGroup: 1100
            seccompProfile:
              type: RuntimeDefault
          volumes:
          - name: local-config
            configMap:
              name: local-config-map
          containers:
          - name: dspace-cli
            image: dataquest/dspace:dspace-7_x
            command: ["/bin/sh", "-c"]
            args:
              - /dspace/bin/dspace health-report -e YOUR.EMAIL@DOMAIN.COM
            resources:
              requests:
                cpu: 1000m
                memory: 1Gi
              limits:
                cpu: 2
                memory: 2Gi
            securityContext:
              allowPrivilegeEscalation: false
              capabilities:
                drop: ["ALL"]
              runAsNonRoot: true
              runAsUser: 1100
            volumeMounts:
            - name: local-config
              mountPath: /dspace/config/local.cfg
              subPath: local.cfg
            env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: dspace-postgres-superuser
                  key: password

---
# OAI Import - Daily at 11 PM
apiVersion: batch/v1
kind: CronJob
metadata:
  name: dspace-oai-import
spec:
  schedule: "0 23 * * *"
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          securityContext:
            runAsNonRoot: true
            runAsUser: 1100
            fsGroup: 1100
            seccompProfile:
              type: RuntimeDefault
          volumes:
          - name: local-config
            configMap:
              name: local-config-map
          containers:
          - name: dspace-cli
            image: dataquest/dspace:dspace-7_x
            command: ["/bin/sh", "-c"]
            args:
              - /dspace/bin/dspace oai import
            resources:
              requests:
                cpu: 1000m
                memory: 1Gi
              limits:
                cpu: 2
                memory: 2Gi
            securityContext:
              allowPrivilegeEscalation: false
              capabilities:
                drop: ["ALL"]
              runAsNonRoot: true
              runAsUser: 1100
            volumeMounts:
            - name: local-config
              mountPath: /dspace/config/local.cfg
              subPath: local.cfg
            env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: dspace-postgres-superuser
                  key: password

---
# Subscription Send (Daily) - Daily at 3:01 AM
apiVersion: batch/v1
kind: CronJob
metadata:
  name: dspace-subscription-daily
spec:
  schedule: "1 3 * * *"
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          securityContext:
            runAsNonRoot: true
            runAsUser: 1100
            fsGroup: 1100
            seccompProfile:
              type: RuntimeDefault
          volumes:
          - name: local-config
            configMap:
              name: local-config-map
          containers:
          - name: dspace-cli
            image: dataquest/dspace:dspace-7_x
            command: ["/bin/sh", "-c"]
            args:
              - /dspace/bin/dspace subscription-send -f D
            resources:
              requests:
                cpu: 1000m
                memory: 1Gi
              limits:
                cpu: 2
                memory: 2Gi
            securityContext:
              allowPrivilegeEscalation: false
              capabilities:
                drop: ["ALL"]
              runAsNonRoot: true
              runAsUser: 1100
            volumeMounts:
            - name: local-config
              mountPath: /dspace/config/local.cfg
              subPath: local.cfg
            env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: dspace-postgres-superuser
                  key: password

---
# Subscription Send (Weekly) - Sundays at 3:02 AM
apiVersion: batch/v1
kind: CronJob
metadata:
  name: dspace-subscription-weekly
spec:
  schedule: "2 3 * * 0"
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          securityContext:
            runAsNonRoot: true
            runAsUser: 1100
            fsGroup: 1100
            seccompProfile:
              type: RuntimeDefault
          volumes:
          - name: local-config
            configMap:
              name: local-config-map
          containers:
          - name: dspace-cli
            image: dataquest/dspace:dspace-7_x
            command: ["/bin/sh", "-c"]
            args:
              - /dspace/bin/dspace subscription-send -f W
            resources:
              requests:
                cpu: 1000m
                memory: 1Gi
              limits:
                cpu: 2
                memory: 2Gi
            securityContext:
              allowPrivilegeEscalation: false
              capabilities:
                drop: ["ALL"]
              runAsNonRoot: true
              runAsUser: 1100
            volumeMounts:
            - name: local-config
              mountPath: /dspace/config/local.cfg
              subPath: local.cfg
            env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: dspace-postgres-superuser
                  key: password

---
# Subscription Send (Monthly) - 1st day of month at 3:03 AM
apiVersion: batch/v1
kind: CronJob
metadata:
  name: dspace-subscription-monthly
spec:
  schedule: "3 3 1 * *"
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          securityContext:
            runAsNonRoot: true
            runAsUser: 1100
            fsGroup: 1100
            seccompProfile:
              type: RuntimeDefault
          volumes:
          - name: local-config
            configMap:
              name: local-config-map
          containers:
          - name: dspace-cli
            image: dataquest/dspace:dspace-7_x
            command: ["/bin/sh", "-c"]
            args:
              - /dspace/bin/dspace subscription-send -f M
            resources:
              requests:
                cpu: 1000m
                memory: 1Gi
              limits:
                cpu: 2
                memory: 2Gi
            securityContext:
              allowPrivilegeEscalation: false
              capabilities:
                drop: ["ALL"]
              runAsNonRoot: true
              runAsUser: 1100
            volumeMounts:
            - name: local-config
              mountPath: /dspace/config/local.cfg
              subPath: local.cfg
            env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: dspace-postgres-superuser
                  key: password

---
# Cleanup - 1st day of month at 4:00 AM
apiVersion: batch/v1
kind: CronJob
metadata:
  name: dspace-cleanup
spec:
  schedule: "0 4 1 * *"
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          securityContext:
            runAsNonRoot: true
            runAsUser: 1100
            fsGroup: 1100
            seccompProfile:
              type: RuntimeDefault
          volumes:
          - name: local-config
            configMap:
              name: local-config-map
          containers:
          - name: dspace-cli
            image: dataquest/dspace:dspace-7_x
            command: ["/bin/sh", "-c"]
            args:
              - /dspace/bin/dspace cleanup
            resources:
              requests:
                cpu: 1000m
                memory: 1Gi
              limits:
                cpu: 2
                memory: 2Gi
            securityContext:
              allowPrivilegeEscalation: false
              capabilities:
                drop: ["ALL"]
              runAsNonRoot: true
              runAsUser: 1100
            volumeMounts:
            - name: local-config
              mountPath: /dspace/config/local.cfg
              subPath: local.cfg
            env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: dspace-postgres-superuser
                  key: password
